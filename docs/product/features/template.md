# Feature name

One or two sentences: what this feature is for, in business terms. Link related features.

- **Screen:** Name in the menu, key `area/screen` (from [screen-catalog.json](../screen-catalog.json)).
- **Decisions:** the ADRs that apply.
- **Read with:** [conventions](../conventions.md), [API conventions](../../api/conventions.md), [glossary](../glossary.md).

<!--
How to use this template:
- Copy it to features/<feature-name>.md (English, kebab-case, plural noun: clients.md, payment-methods.md).
- Pick a rule ID prefix of 3-5 capital letters that no other document uses (e.g. CLI for clients) and list it in docs/README.md.
- Keep only the sections that apply. Keep the order.
- Rules describe decided behavior. Anything undecided goes to ../open-questions.md, never here.
- Messages are in Portuguese, verbatim. Everything else is in English.
- Add every new business term to ../glossary.md.
- Delete this comment.
-->

## Data model

Collection `name`. Besides the fields below, every record has the audit fields and `version` ([conventions](../conventions.md#authorship-audit)).

| Field | Type | Required | Format and notes |
|---|---|---|---|
| `id` | text | yes | GUID generated by the server (CNV-04) |

```json
{
  "id": "0192f1a4-7c3e-7b21-9f5a-2d8e4c1b6a70"
}
```

## Validation on save

| ID | Field | Rule | Message |
|---|---|---|---|
| XXX-01 | `field` | Rule, stated so that a test can check it. | Mensagem exibida, em português. |

## Behavior

Rules that are not field validations: lifecycle, calculations, restrictions.

| ID | Rule |
|---|---|
| XXX-02 | Rule. |

## Messages

Verbatim, in Portuguese.

| Situation | Message |
|---|---|
| Record saved | Mensagem. |

## Operations

Minimum levels follow [permission profiles](permission-profiles.md#what-each-level-allows) unless a rule above documents an exception.

| Front-end method | Route | Minimum level | Notes |
|---|---|---|---|
| `listThings({ page, pageSize, search })` | `GET /api/v1/things` | Reader | Paged |

## Errors

| Status | `code` | When |
|---|---|---|
| 400 | `validation_failed` | Any rule in validation on save |
